{
  "name": "chat-service-ws-messaging",
  "version": "0.5.1",
  "description": "chat-service-ws-messaging",
  "main": "index.js",
  "private": false,
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "compile": "babel src --out-dir lib",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "generate-docs": "jsdoc --configure .jsdoc.json --verbose",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "test": "nyc mocha"
  },
  "author": "Andrey Sharapov <an.sh.1@yandex.ru> (https://github.com/an-sh)",
  "repository": {
    "type": "git",
    "url": "https://github.com/an-sh/chat-service-ws-messaging.git"
  },
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.6",
    "emitter-pubsub-broker": "^0.6.0",
    "lodash.assign": "^4.2.0",
    "semver": "^5.3.0",
    "ws-messaging": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015-node4": "^2.1.0"
  },
  "peerDependencies": {
    "chat-service": ">= 0.12.0 < 2"
  },
  "nyc": {
    "include": [
      "src/**/*.js"
    ],
    "reporter": [
      "lcov",
      "text-summary"
    ]
  }
}
